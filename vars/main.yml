---
# Packages you want to install on the system
packages:
  - unattended-upgrades
  - network-manager-openvpn-gnome
  - notepadqq
  - python3-pip
  - pipx

username: jalex
slack_version: 4.41.105 # Find the version here: https://slack.com/intl/en-gb/release-notes/linux

# Git settings
git_email: jalex@jalexmatey.com
git_username: jalexmatey

# For linuxbrew
lb__owner: "{{ username }}"
lb__group: "{{ username }}"
lb__packages:
  - name: "starship"
    state: "present"
